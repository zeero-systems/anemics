
export enum OperatorEnum {
  EQ = "eq",
  LT = "lt",
  GT = "gt",
  LIKE = "like",
  BETWEEN = "between",
  IN = "in",
  "NOT IN" = "not in",
  "IS NULL" = "is null",
  "IS NOT NULL" = "is not null",
  EXISTS = "exists",
}

export default OperatorEnum