
export enum OperatorEnum {
  EQ = "EQ",
  LT = "LT",
  GT = "GT",
  LIKE = "LIKE",
  BETWEEN = "BETWEEN",
  IN = "IN",
  NOT_IN = "NOT IN",
  IS_NULL = "IS NULL",
  IS_NOT_NULL = "IS NOT NULL",
  EXISTS = "EXISTS",
}

export default OperatorEnum